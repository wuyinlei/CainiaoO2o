<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
 <mapper namespace = "com.ruolan.o2o.dao.ProductCategoryDao">
 		<select id = "queryProductCategoryList" resultType="com.ruolan.o2o.entity.ProductCategory"
				parameterType="Long">
			<!-- 具体的sql -->
			SELECT
			product_category_id,
			product_category_name,
			product_category_desc,
			priority,
			create_time,
			last_edit_time,
			shop_id
			FROM
			tb_product_category
			WHERE
			shop_id = #{shopId}
			ORDER by priority DESC
		</select>
 </mapper>